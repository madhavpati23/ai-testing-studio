streamlit==1.45.1
pandas>=2.0
PyYAML>=6.0
anthropic>=0.40.0
openpyxl>=3.1
pypdf>=4.0
Pillow>=10.0
# Optional: enables "Sign in with Google" (st.login OIDC). Login is off unless an
# [auth] provider is configured in .streamlit/secrets.toml — the app runs fine without it.
Authlib>=1.3.2
# Optional: only needed for the multi-tenant Postgres history backend
# (set PRS_STUDIO_DB_URL). The default local history uses the stdlib sqlite3.
# psycopg[binary]>=3.1
# The framework packages are bundled in ./vendor (see _bootstrap.py), so this app
# is self-contained — it deploys from a PRIVATE repo with no external git deps.
# redeploy: 2026-07-10c
